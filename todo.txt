GAME:

ADD:
- add enemy that can teleport
- add enemy that can jump towers
- add a turret that smashes enemy once placed (or maybe its just a hammer that doesnt get placed)

FIX:
- preview of the turret is broken




LEVEL EDITOR:

- add a way to see all the levels available (with a menu), so that i can click on one of them and modify it
- make it more clear when a new level is created which level it will be (for example if i have level 1 and level 2, if create a new level it would be level 3 but this is not show in the ui)